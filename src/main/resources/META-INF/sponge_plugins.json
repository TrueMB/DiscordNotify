{
    "loader": {
        "name": "java_plain",
        "version": "1.0"
    },
    "license": "GPL-2.0",
    "plugins": [
        {
            "id": "${project.artifactId}",
            "name": "${project.name}",
			"version": "${project.version}",
            "entrypoint": "me.truemb.universal.minecraft.main.SpongeMain",
            "description": "${project.description}",
			"dependencies": [
				{
					"id": "spicord",
					"version": "4.2.1",
					"load-order": "before",
					"optional": true
				}
			]
        }
    ]
}